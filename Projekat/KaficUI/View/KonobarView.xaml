<UserControl x:Class="KaficUI.View.KonobarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KaficUI.View"
             xmlns:Projekat="clr-namespace:Projekat;assembly=Projekat"            
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <CollectionViewSource x:Key="konobarViewSource" d:Source  ="{d:DesignInstance {x:Type Projekat:Konobar}, CreateList=True}"/>
    </UserControl.Resources>
    <Grid DataContext="{StaticResource konobarViewSource}">
        <DataGrid ItemsSource="{Binding}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID zaposlenog"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
